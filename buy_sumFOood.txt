#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define ld long double
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int tc;
    cin >> tc;
    while(tc--){
        int s;
        cin >> s;
        int ans = s;
        while(s > 0){
            s-=10;
            s++;
            if(s > 0){
                ans++;
            }
        }
        cout << ans << endl;
    }
    
}
#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int t;
    cin >> t;
    while(t--)
    {
        long long int  n;
        cin >> n;
        long long int  final {1};
        // for(long long int i {n}; i > 0; i--){
        //     final *= i;
        // }
        cout << n << endl;
    }
}
def BellmanFord(Graph ans):
	array ans = [] #empty array to infinity
	ans[source] = 0
	for i from 1 to |V|—1
		for edge in G.edges():
			if(ans[edge.from()] + ans[edge.weight()] < ans[edge.to()]):
				ans[edge] = edge.weight()

	return ans
#include <bits/stdc++.h>
using namespace std;
using ll = long long;
using ld = long double;

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);
    
    int tc = 1;
    while(tc--){
        int n, k;
        int new_socks {0};
        cin >> n >> k;
        
        cout << n+(n-1)/(k-1) << endl;
    }
    return 0;
}
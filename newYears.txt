#include <bits/stdc++.h>
using namespace std;
using ll = long long;
using ld = long double;

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);
    
    int tc = 1;
    cin >> tc;
    
    while(tc--){
        int n;
        bool divisible {false};
        int factor {n + n/2020};
        cin >> n;
        
        if(n >= n - (n % 2020) && n <= n - (n % 2020) + (n/2020)){
            cout << "YES" << "\n";
        } else{
            cout << "NO" << "\n";
        }
    }
    return 0;
}
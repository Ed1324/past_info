#include <bits/stdc++.h>
using namespace std;
#define MAXN 200005
#define int long long
#define ld long double
#define endl "\n"

void solve(){
    int n, x; cin >> n >> x;
    int found = 0;
    map<int, int>a;
    for(int i = 1; i <= n; i++) {
        int te; 
        cin >> te;
        int complement = x - te;
        if(a.count(complement)) {
            cout << i << " " << a[complement] << " " << endl;
            exit(0);
        }
        a[te] = i; 
    }
    cout << "IMPOSSIBLE" << endl;
}



signed main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout << endl;
    solve();
    return 0;
}

